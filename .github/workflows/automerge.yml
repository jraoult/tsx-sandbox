name: Merge me!
on:
  workflow_run:
    workflows: [ "CI" ]
    types:
      - completed
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: Merge me!
        uses: ridedott/merge-me-action@v2
        with:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
